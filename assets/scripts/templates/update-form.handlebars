<div class="container text-center h-100">
  <div class="card-body">
    <h5 class="card-title">{{upload.title}}</h5>
    <form data-id="{{upload._id}}" class="col lg-2 update-form">
      <div class="form-group">
        <input type="text" name="tag" class="form-control" required="required" value="{{upload.tag}}">
      </div>
      <div class="form-group">
        <input class="btn btn-primary btn-block" type="submit" value="Update">
      </div>
    </form>
  </div>
</div>
